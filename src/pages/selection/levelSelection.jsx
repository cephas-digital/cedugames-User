import{useEffect,useState}from"react";import{Link,useSearchParams}from"react-router-dom";import Navbar from"../../components/navbar";import WelcomeCard from"../../components/welcomeCard";import welcome from"../../assets/image.png";import{apiRequest}from"../../services/api";
export default function LevelSelection(){const[params]=useSearchParams(),ageId=params.get("ageGroup"),categoryId=params.get("category"),[levels,setLevels]=useState([]),[loading,setLoading]=useState(Boolean(categoryId)),[error,setError]=useState(categoryId?"":"Select a category first.");useEffect(()=>{if(!categoryId)return;apiRequest(`/catalog/categories/${categoryId}/levels`).then(d=>setLevels(d.levels||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[categoryId]);return <div className="min-h-screen bg-gray-100"><Navbar/><div className="px-3 sm:px-6 lg:px-10 py-4 sm:py-8 space-y-8"><WelcomeCard image={welcome} header="Choose Your Level" text="Pick a level and continue your learning adventure."/><div className="rounded-2xl bg-white p-6 sm:p-8"><h2 className="mb-6 text-xl font-bold text-gray-700">Available levels</h2>{loading?<p className="py-16 text-center text-gray-400">Loading levels...</p>:error?<p className="rounded-xl bg-red-50 p-4 text-red-600">{error}</p>:levels.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{levels.map(x=><Link key={x.id} to={`/play?ageGroup=${ageId}&category=${categoryId}&level=${x.id}`} className="rounded-2xl border border-purple-100 bg-purple-50 p-6 transition hover:-translate-y-1 hover:shadow-lg"><span className="rounded-full bg-purple-600 px-3 py-1 text-xs font-bold text-white">Level {x.level_number}</span><h3 className="mt-5 text-xl font-bold text-gray-800">{x.name}</h3><p className="mt-2 text-sm leading-6 text-gray-600">{x.description||"Start this learning level."}</p><p className="mt-5 font-bold text-purple-600">Play level →</p></Link>)}</div>:<p className="py-16 text-center text-gray-400">No levels are available in this category yet.</p>}</div></div></div>}
