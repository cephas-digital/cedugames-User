import nextLevel from "../../assets/next-level.png";

export default function ResultModal({isCorrect}){
 const saved=isCorrect==="saved",positive=isCorrect===true;
 const styles=saved?{border:"border-amber-400",background:"bg-amber-500",text:"text-amber-600"}:positive?{border:"border-emerald-400",background:"bg-emerald-500",text:"text-emerald-600"}:{border:"border-red-400",background:"bg-red-500",text:"text-red-600"};
 return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4 backdrop-blur-sm" role="dialog" aria-live="assertive" aria-label={saved?"Answer saved":positive?"Correct answer":"Wrong answer"}><div className={`w-full max-w-sm animate-scaleIn rounded-3xl border-4 bg-white p-7 text-center text-slate-900 shadow-2xl ${styles.border}`}><div className={`mx-auto grid h-20 w-20 place-items-center rounded-full text-5xl font-black text-white ${styles.background}`}>{saved?"✓":positive?"✓":"×"}</div><h2 className={`mt-4 text-3xl font-black ${styles.text}`}>{saved?"Answer saved":positive?"Correct!":"Not quite!"}</h2><p className="mt-2 text-sm font-semibold text-slate-500">{saved?"Your answer will be scored securely at the end of the level.":positive?"Great job — keep going!":"Good try — the next question is coming up."}</p>{positive&&<img src={nextLevel} alt="" className="mx-auto mt-4 max-h-36 object-contain"/>}<div className="mx-auto mt-5 h-1.5 w-full overflow-hidden rounded-full bg-slate-100"><div className={`h-full animate-[feedbackTimer_1.5s_linear_forwards] ${styles.background}`}/></div></div></div>;
}
