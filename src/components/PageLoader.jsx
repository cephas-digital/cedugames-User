import { BrandLogo } from "./Brand";
export default function PageLoader({message="Preparing your next adventure…",cards=3,className=""}){return <section className={`page-loader ${className}`} role="status" aria-live="polite"><div className="page-loader__brand"><BrandLogo className="page-loader__logo"/><span className="brand-loader__dots" aria-hidden="true"><i/><i/><i/></span><h2>{message}</h2><p>Good things are getting ready for you.</p></div><div className="page-loader__cards" aria-hidden="true">{Array.from({length:cards},(_,index)=><div key={index} className="page-loader__card" style={{animationDelay:`${index*100}ms`}}><span/><i/><i/><b/></div>)}</div></section>}
