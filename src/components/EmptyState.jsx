import { Link } from "react-router-dom";
export default function EmptyState({title="Nothing here yet",message="New learning content will appear here when it is ready.",actionLabel="Go back",actionTo="/age-selection",icon="📚"}){return <section className="empty-state" role="status"><div className="empty-state__icon" aria-hidden="true">{icon}</div><span className="empty-state__spark empty-state__spark--one" aria-hidden="true">✦</span><span className="empty-state__spark empty-state__spark--two" aria-hidden="true">✦</span><h2>{title}</h2><p>{message}</p><Link to={actionTo} className="empty-state__action">← {actionLabel}</Link></section>}
